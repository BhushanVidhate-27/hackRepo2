# Production: set to your API origin (no trailing slash), e.g. https://api.example.com
VITE_API_BASE_URL=

# Optional: Sentry browser DSN for client error reporting
VITE_SENTRY_DSN=

# Optional: forwarded as X-Tenant-Id on API requests when using shared backends
VITE_TENANT_ID=
